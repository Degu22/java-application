pipeline {
    agent any
    
    tools {
        maven 'maven'
    }


    stages {
	
        stage('checkout') {
            steps {
                echo 'git checkout stage'
				git branch: 'main', url: 'https://github.com/Degu22/java-application.git'
            }
        }

        stage('build') {
            steps {
                echo 'Building with maven '
				sh 'mvn clean install '
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying to tomcat'
				deploy adapters: [tomcat9(alternativeDeploymentContext: '', credentialsId: 'tomcat', path: '', url: 'http://http://3.94.100.156:8081/')], contextPath: 'insta', war: '**/*.war'
				
            }
        }		
		
    }
}
